- name: Crontab wireguard backup
  cron:
    name: 'Wireguard configuration backup'
    minute: '0'
    hour: '2'
    user: root
    job: 'sudo -u pi rsync -a --delete-after {{ home_directory }}/docker_config/{{ item.container }} /mnt/md0/docker_config/'